# ONI character source

The production ONI AI chamber intentionally imports `oni-character.webp`. The binary at that path is the approved ONI character source so the chamber, SSR bundle, and web rig all resolve the same asset without a stale parallel import path.
